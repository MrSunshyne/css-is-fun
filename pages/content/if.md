# If

The `if()` function is a CSS function that allows you to conditionally apply styles based on a boolean expression.

## Syntax

```css
if(condition, true, false)
```

## Examples

<BaselineChecker feature-name="if" />
